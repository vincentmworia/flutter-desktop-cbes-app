import 'package:flutter/material.dart';

class PipingUnitScreen extends StatelessWidget {
  const PipingUnitScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("""
     PIPING UNIT SCREEN
      """),
    );
  }
}
