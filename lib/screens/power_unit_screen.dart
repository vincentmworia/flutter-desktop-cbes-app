import 'package:flutter/material.dart';

class PowerUnitScreen extends StatelessWidget {
  const PowerUnitScreen({Key? key}) : super(key: key);


  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Power Unit Screen"),
    );
  }
}
